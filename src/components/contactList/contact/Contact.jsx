

const Contact = ({name,number}) => {
  return (
    <div>
        <p>name:{name}</p>
        <p>number:{number}</p>
        <button type='button'>Delete</button>
    </div>
  )
}
export default Contact